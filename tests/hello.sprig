shape Counter:
    n: number

define greet(name):
    give back "Hello, " + name + "!"

let msg = greet("Sprig")
print(msg)

let c = Counter { n: 0 }
as long as c.n < 3:
    print(c.n)
    c.n = c.n + 1
